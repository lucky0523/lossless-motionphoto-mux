# ---------------------------------------------------------------------------
# 数据源与输出：体积大、且是私人照片，不进仓库
# ---------------------------------------------------------------------------
DCIM/
motionphoto_output/
staticphoto_output/
movieout/
output/

# --single-out 用别的目录名时，把它补到上面；下面这条兜住写文件时的中间态
*.part

# 每次运行生成的报告
motionphoto_report*.txt

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# ---------------------------------------------------------------------------
# 操作系统 / 编辑器
# ---------------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*~

# ---------------------------------------------------------------------------
# 说明
# ---------------------------------------------------------------------------
# 真机样片（OPPO_sample.jpg / Xiaomi_sample.jpg / Samsung_sample.jpg）是
# README 里格式分析的依据，**故意不忽略**，需要跟随仓库一起提交。
#
# 注意：这三张是相机直出照片，EXIF 里可能带 GPS 坐标和机型信息。公开仓库前
# 请自行确认是否愿意公开这些信息，可以用下面的命令检查：
#     exiftool -GPS:all -Make -Model *_sample.jpg
# 如需清除 GPS（会改动样片，之后 README 里的字节数需重新核对）：
#     exiftool -GPS:all= -overwrite_original *_sample.jpg
